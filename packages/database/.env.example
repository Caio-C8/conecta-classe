# Crie um arquivo .env com essa variável, subsitua os valores se tiver os valores das variáveis do .env da raiz (./.env) for diferente

DATABASE_URL="postgresql://seu_usuario:senha@localhost:5432/nome_banco?schema=public"